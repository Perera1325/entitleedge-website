import React from 'react';
import ContactForm from './ContactForm';

export const metadata = {
  title: "Contact EntitleEdge Advisory | Planning & Claims Support",
  description: "Send us an enquiry for programme support, delay analysis, EOT assistance or commercial administration.",
};

export default function ContactPage() {
  return <ContactForm />;
}
